pipeline{
    agent { label 'agent1' }
    stages{
        stage("hello"){
            steps{
                echo "done done"
            }
        }
        stage("deploy"){
            steps{
                echo "this is deployingfffffffffffffffff the code"
            }
        }
    }
}
